{
  "name": "crm-app",
  "version": "1.0.0",
  "description": "CRM application with React frontend and Node.js API",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "cd frontend && npm start",
    "dev": "cd api && node index.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}